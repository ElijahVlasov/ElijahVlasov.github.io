{
  "dependencies": {
    "@owickstrom/the-monospace-web": "github:owickstrom/the-monospace-web",
    "@rescript/core": "^1.6.1",
    "rescript": "^11.1.4"
  },
  "scripts": {
    "build-jekyll": "bundle exec jekyll build",
    "build": "npm ci && mkdir -p _site/the-monospace-web && cp node_modules/@owickstrom/the-monospace-web/src/* _site/the-monospace-web && mkdir -p _site/images && cp images/* _site/images",
    "res:build": "rescript",
    "res:dev": "rescript -w"
  }
}
